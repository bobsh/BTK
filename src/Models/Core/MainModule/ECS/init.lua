--- ECS Library
--
-- @classmod ECS

local ECS = {
    --- @{ECS.BaseComponent}
    BaseComponent = require(script.BaseComponent),

    --- @{ECS.Entity}
    Entity = require(script.Entity),
}

return ECS